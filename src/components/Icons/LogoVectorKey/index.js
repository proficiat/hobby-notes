import styled from 'styled-components'

import { ReactComponent as Logo } from '../../../assets/logo_vector_key.svg'

const LogoVectorKey = styled(Logo)`
  display: block;
  width: 220px;
  height: 220px;
  overflow: inherit;
  cursor: pointer;
`

export default LogoVectorKey
